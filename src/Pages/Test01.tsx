import React from 'react'
import { useSelector } from 'react-redux'

function Test01() {
  // const {numReducer}:{numReducer:number} = useSelector((state:ReducerType)=>{console.log(state,"state"); return state})
  return (
    <>
    <div>Test01</div>
    {/* {JSON.stringify(numReducer)} */}
    </>
  )
}

export default Test01